var React = require('react');

module.exports = React.createClass({

  render: function () {
    return (
      <nav className="Navigation">
        <a>Zone module</a>
        <a>User module</a>
      </nav>
    );
  }
});
